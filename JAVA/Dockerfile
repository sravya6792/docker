# Use a lightweight base image
FROM alpine:latest

# Set the working directory (optional)
WORKDIR /app

# You can copy any files you need (optional)
# COPY your-file.txt .

# Use CMD to run a shell
CMD ["date"]

